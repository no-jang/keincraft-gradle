package extensions

abstract class AioExtension {
/*    abstract val mappings: ListProperty<AioMapping>

    fun srcPackage(pkg: String = "") {
        srcMappings.add(AioMapping(pkg, "src/main/java/$pkg"))
    }

    fun srcResources(pkg: String = "") {
        srcMappings.add(AioMapping(pkg, "src/main/resources/$pkg"))
    }

    fun testPackage(pkg: String = "") {
        testMappings.add(AioMapping(pkg, "src/test/java/$pkg"))
    }

    fun testResources(pkg: String = "") {
        testMappings.add(AioMapping(pkg, "src/test/resources/$pkg"))
    }*/
}